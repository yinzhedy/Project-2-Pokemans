{{#each cards as |card| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/project/{{card.id}}">{{card.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount card.type}}</span>
      needed!
    </p>
    <p>Created by {{card.user.name}} on {{format_date card.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{card.description}}
    </p>
  </div>
</div>
{{/each}}
